package org.saturn.generator.messageconsructor.parser.strategy.seporator

import org.saturn.generator.messageconsructor.parser.strategy.ParseStrategy
import org.saturn.generator.usefuldata.UsefulData

class SeporatorParseStrategy : ParseStrategy {
    override fun check(data: List<Int>): Boolean {
        SeparatorParseStrategy
    }

    override fun parse(data: List<Int>): UsefulData {
        TODO("Not yet implemented")
    }
}